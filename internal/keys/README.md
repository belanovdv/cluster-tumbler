# internal/keys

Генератор ETCD-ключей. Отделен от `internal/etcd`

- Модуль `keys` отвечает за доменную схему ключей
- Модуль `etcd` отвечает за транспортные операции


Основной корень:

```text
/{cluster_id}/cluster